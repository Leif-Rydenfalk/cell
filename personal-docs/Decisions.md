We need to remove the boundary between local system and network - there should be no such thing. the same exact api must work wiht zero copy sub micro second latency on the same machine and with 200 milliseconds cross continent without any code change or config change whatsoever. It must be truly p2p host to host - the orchestrators needs to run once per host computer and communicate with eachother using the tissue functionality.